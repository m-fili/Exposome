/* Variable records — domain 3: Family, Household, and Social Relationships.
 *
 * No records yet. Add them here keyed by the dotted node id from
 * data/taxonomy.js, e.g. "social.disconnection.loneliness". Open the variable
 * in the explorer and use "Copy blank record" to get a ready-to-paste stub with
 * every field present.
 *
 * See data/variables/health.js and socioeconomic.js for worked examples.
 */

window.EXPOSOME_VARIABLES = Object.assign(window.EXPOSOME_VARIABLES || {}, {

  // "social.disconnection.loneliness": { ... },

});
